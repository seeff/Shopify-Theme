<!DOCTYPE html>
<!--[if lt IE 7]><html class="no-js lt-ie9 lt-ie8 lt-ie7" lang="en"> <![endif]-->
<!--[if IE 7]><html class="no-js lt-ie9 lt-ie8" lang="en"> <![endif]-->
<!--[if IE 8]><html class="no-js lt-ie9" lang="en"> <![endif]-->
<!--[if IE 9 ]><html class="ie9 no-js"> <![endif]-->
<!--[if (gt IE 9)|!(IE)]><!--> <html class="no-js"> <!--<![endif]-->  
<head>
  <meta charset="utf-8" />
  <!--[if IE]><meta http-equiv='X-UA-Compatible' content='IE=edge,chrome=1' /><![endif]-->

  <title>
    {% if template == 'index' %}{% if page_title contains 'Welcome' %}{{ shop.name }} &ndash; Welcome{% else %}{{ page_title }}{% endif %}{% elsif template == '404' %}Page Not Found{% elsif page_title contains shop.name %}{{ page_title }}{% else %}{{ page_title }} &ndash; {{ shop.name }}{% endif %}
  </title>
  {% if page_description %}
  <meta name="description" content="{{ page_description }}" />
  {% endif %}
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <link rel="canonical" href="{{ canonical_url }}" />
  <link rel=”alternate” hreflang=”es” href=” http://es.unyq.com” />
  <link rel=”alternate” hreflang=”de” href=” http://de.unyq.com” />
  <link rel=”alternate” href=”http://unyq.com” hreflang=”x-default” />
  {% if product.tags contains 'no-index' %}
    <meta name="robots" content="noindex, follow" />
  {% endif %}
  
  {% comment %}<meta name="viewport" content="width=device-width" />{% endcomment %}
  
  {% include 'open-graph-tags' %}

  {{ content_for_header }}
  <link rel="shortcut icon" type="image/png" href="//cdn.shopify.com/s/files/1/0412/3109/t/2/assets/favicon.ico"/>
  
  <script type="text/javascript" src="//use.typekit.net/nqg1cng.js" ></script>
  <script type="text/javascript">try{Typekit.load();}catch(e){}</script>


  {{ 'style.css' | asset_url | stylesheet_tag }}
  {% comment %}{{ 'wufoo_form.css' | asset_url | stylesheet_tag }}{% endcomment %}

  <!--[if lt IE 9]>
  {{ '//html5shiv.googlecode.com/svn/trunk/html5.js' | script_tag }}
  <![endif]-->

  {% if template contains 'customers' %}
  {{ 'shopify_common.js' | shopify_asset_url | script_tag }}
  {{ 'customer_area.js'  | shopify_asset_url | script_tag }}
  {% endif %}
  
  {% if template contains 'cart' %}
     {% include 'facebook-cart' %}
  {% endif %}
  
  {% comment %}
  Needed when shopify_common.js is used.
  {% endcomment %}
  <!--[if lt IE 8]>
  {{ 'json2.js' | shopify_asset_url | script_tag }}
  <![endif]-->

  <script tye="text/javascript" src="{{ 'option_selection.js' | shopify_asset_url }}" async></script>

  {{ '//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js' | script_tag }}
  {{ 'api.jquery.js' | shopify_asset_url | script_tag }}
  
  

  <script type="text/javascript" src="{{ 'shop.js'  | asset_url }}"></script>

    
 
</head>




{% if settings.language == "en" %}

	{% assign search_text = settings.search_en %}
	{% assign login = settings.login_en %}
    {% assign logout = settings.logout_en %}
    {% assign my_account = settings.my_account_en %}

    {% assign cart_text = settings.cart_en %}
    {% assign view_cart = settings.view_cart_en %}
    {% assign subtotal = settings.subtotal_en %}

	{% assign price_text = settings.price_en %}
    {% assign qty_text = settings.qty_en %}
    {% assign remove_text = settings.remove_en %}


	{% assign mailing_list = settings.mailing_list_en %}
	{% assign enter_email = settings.enter_email_en %}

	{% assign rights = settings.rights_en %}

{% elsif settings.language == "es" %}

	{% assign search_text = settings.search_es %}
	{% assign login = settings.login_es %}
    {% assign logout = settings.logout_es %}
    {% assign my_account = settings.my_account_es %}

    {% assign cart_text = settings.cart_es %}
    {% assign view_cart = settings.view_cart_es %}
    {% assign subtotal = settings.subtotal_es %}

	{% assign price_text = settings.price_es %}
    {% assign qty_text = settings.qty_es %}
    {% assign remove_text = settings.remove_es %}

	{% assign mailing_list = settings.mailing_list_es %}
	{% assign enter_email = settings.enter_email_es %}

	{% assign rights = settings.rights_es %}

{% elsif settings.language == "de" %}

	{% assign search_text = settings.search_de %}
	{% assign login = settings.login_de %}
    {% assign logout = settings.logout_de %}
    {% assign my_account = settings.my_account_de %}

    {% assign cart_text = settings.cart_de %}
    {% assign view_cart = settings.view_cart_de %}
    {% assign subtotal_text = settings.subtotal_de %}

	{% assign price_text = settings.price_es %}
    {% assign qty_text = settings.qty_es %}
    {% assign remove_text = settings.remove_es %}

	{% assign mailing_list = settings.mailing_list_de %}
	{% assign enter_email = settings.enter_email_de %}

	{% assign rights = settings.rights_de %}

{% endif %}

<body id="{{ page_title | handle }}" class="{% if customer %}customer-logged-in {% endif %}template-{{ template | replace: '.', ' ' | truncatewords: 1, '' | handle }}" >


	
    <header>
      <div id="top-hat" class="cf">
		<div class="c ar">
		  <form action="/search" method="get" class="search-form l hidemobile" role="search">
		    <input id="search-icon" type="image" src="{{'search-icon.svg' | asset_url}}" width="14" height="14" />
            <input name="q" type="text" id="search-field" placeholder="{{search_text}}" class="hint" />
	        <input type="hidden" name="type" value="product" />
	        
	      </form>
	       
	      
	    <div id="top-right">
          <a href="tel:18662869773">+1 (866) 286-9773</a>
          <div id="countries">
            <div id="current-country" class="en"><img src="{{ 'flag-en.gif' | asset_url }}" alt="UNYQ English Site" width="23" height="15" /></div>
            <ul id="country-list">
              <li><a href="http://de.unyq.com"><img src="{{ 'flag-de.gif' | asset_url }}" alt="UNYQ German Deutschland" width="23" height="15" /> Deutsch</a></li>
              <li><a href="http://es.unyq.com"><img src="{{ 'flag-es.gif' | asset_url }}" alt="UNYQ Spain Espanol" width="23" height="15" /> Espanol</a></li>
            </ul>
          </div>
          
          {% include 'currencies-switcher' %}
            
            
	      {% if shop.customer_accounts_enabled %}
	      <p class="customer-links hidemobile"> 
	        {% if customer %}
            <a href="/account">{{my_account}}</a> |  {{ logout | customer_logout_link }}
	        {% else %}
	          {{ login | customer_login_link }}
	        {% endif %}
	      </p>
	      {% endif %}

			
          {% if cart.item_count > 0 %}
	      <div id="top-cart" class="hidemobile">
            <a href="/cart" id="active-cart">{{cart_text}} <img src="{{'cart-icon.svg' | asset_url }}" width="24" height="24" />{{ cart.item_count }}</a>
			<div id="cart-hover">
					<ul>
					{% for item in cart.items %}
						<li class="cf">
							<div class="forty l">
							<a href="{{item.product.url}}"><img src="{{ item.product.images.first | product_img_url: 'medium' }}" alt="{{ item.title | escape }}" width="91" height="136" /></a>
				    		</div>
				    		<div class="sixty r">
				    			<span class="carttitle"><b><a href="{{item.product.url}}">{{ item.product.title }}</a></b></span>
				    			{% if item.variant.option1 != 'Default Title'%}
								  {% for i in (1..item.product.options.size) %}
								    {% case i %}
								    {% when 1 %}
		                              <span>	
		                              {% if item.product.options.first != "Title" %}
								      <b>{{ item.product.options.first }}:</b>
                                      {% endif %}
                                      {{ item.variant.option1 }}
                              		  </span>
								    {% when 2 %}			
								      {% if item.product.options.size == 2 %}
								        <span><b>{{ item.product.options.last}}:</b> {{ item.variant.option2 }}</span>
								      {% else %}
								        <span><b>{{ item.product.options | remove : item.product.options.first | remove: item.product.options.last  }}:</b> {{ item.variant.option2 }}</span>
								      {% endif %}
								    {% else %}  
								      <span><b>{{ item.product.options.last }}:</b> {{ item.variant.option3 }}</span>
								    {% endcase %}
								  {% endfor %}
								{% endif %}
                              <span class="cartprice"><b>{{price_text}}:</b> {{ item.variant.price | money }}</span>
                              <span class="cartquantity"><b>{{qty_text}}:</b> {{ item.quantity }}</span>
                              <a href="/cart/change?line={{ forloop.index }}&quantity=0" class="cartremove" title="remove">x {{remove_text}}</a>
				    		</div>
						</li>
					{% endfor %}
					</ul>
              <h4 class="subtotal">{{subtotal_text}}: {{ cart.total_price | money }}</h4>
              <a href="/cart" class="btn view-cart">{{view_cart}}</a>
			</div>
	      </div>
          {% endif %}
          
          
	      </div>
		</div>
	</div>
      

      
   <div id="header">
     
    <div id="logo" class="c cf">
      {% comment %}<a href="/products/below-knee-prosthetic-covers" ><img src="{{'bk-announcement-graphic.png' | asset_url}}" id="bk-announcement" /></a>{% endcomment %}
      {% comment %}<a href="https://www.crowdtilt.com/campaigns/awesome-prosthetic-leg-cover-for-rightfooted" ><img src="{{'rightfooted.png' | asset_url}}" id="bk-announcement" /></a>{% endcomment %}
      
      <a href="/" role="banner">
        <img src="{{ 'UNYQ-logo2.svg' | asset_url }}"  alt='Unique Prothetic Covers (UNYQ Logo)' id="logo-img" width="250" height="115" />
      </a><!-- #logo -->
      
      <div id="header-fb" class="hidemobile fb-like" data-href="https://www.facebook.com/pages/UNYQ/673329516059397" data-layout="button_count" data-action="like" data-show-faces="false" data-share="false"></div>
    </div>
     
      
     
      
      <nav id="main-nav" role="navigation" class="hidemobile">
      <div class="c cf">
        <ul class="nav">
          {% for link in linklists.main-menu.links %}
          {% assign has_sub_menu = false %}
          {% assign has_sub_categories = false %}
          {% assign parent_link_active = false %}
          {% assign child_list_handle = link.title | handle %}
          {% if linklists[child_list_handle] and linklists[child_list_handle].links.size > 0 %}
            {% assign has_sub_menu = true %}
            {% for l in linklists[child_list_handle].links %}
              {% if page_title == l.title %}
                {% assign parent_link_active = true %}
              {% endif %}
            {% endfor %}
          {% elsif link.type == 'collection_link' and link.object.all_tags.size > 0 %}
            {% assign has_sub_categories = true %}
          {% endif %}
          <li class="nav-item{% if forloop.first %} first{% elsif forloop.last %} last{% endif %}{% if link.active or parent_link_active %} active{% endif %}{% if has_sub_menu or has_sub_categories %} has-dropdown{% endif %}">
            {{ link.title | link_to: link.url }}
            {% if link.handle contains 'shopaholic' %}
            <div id="shop-dd">
            	<b>Shop By</b>
            	<ul class="cf">
            	{% for link in linklists.shop.links %}
            	<li><a href="{{link.url}}">{{link.title}}</a></li>
            	{% endfor %}
            	</ul>
            	<a href="/collections/all">Shop All</a>
            </div>
            {% endif %}
            {% comment %}
            {% if has_sub_menu or has_sub_categories %}
            
            <ul class="sub-nav">
              <li class="sub-nav-niblet"></li>
              {% if has_sub_menu %}
                {% for l in linklists[child_list_handle].links %}
                <li class="sub-nav-item{% if forloop.first %} first{% elsif forloop.last %} last{% endif %}{% if l.active %} active{% endif %}">
                  <a class="{% if forloop.first %}first{% elsif forloop.last %}last{% endif %}" href="{{ l.url }}">{{ l.title }}</a>
                </li>
                {% endfor %}
              {% elsif has_sub_categories %}
                {% for tag in link.object.all_tags %}
                <li class="sub-nav-item{% if forloop.first %} first{% elsif forloop.last %} last{% endif %}{% if current_tags contains tag %} active{% endif %}">
                  <a class="{% if forloop.first %}first{% elsif forloop.last %}last{% endif %}" href="{{ link.url }}/{{ tag | handle }}">{{ tag }}</a>
                </li>
                {% endfor %}
              {% endif %}
              
            </ul>

            {% endif %}
            {% endcomment %}
          </li>
          {% endfor %}
        </ul>
      </div>
      </nav>
     
     
     <div class="cf showmobile">
       <a href="#" id="menu-btn"><i class="fa fa-bars"></i> Menu</a>
       <a href="/cart" id="mobile-cart-btn">Cart <i class="fa fa-shopping-cart"></i> {{ cart.item_count }}</a>
    </div>
     
       <nav id="mobile-nav" role="navigation" class="showmobile">
        <div class="cf">
          <ul class="nav">
          {% for link in linklists.mobile.links %}
            <li class="nav-item{% if forloop.first %} first{% elsif forloop.last %} last{% endif %}{% if link.active %} active{% endif %}">
              {{ link.title | link_to: link.url }}
            </li>
            {% endfor %}
          </ul>
          
        </div>
        </nav>   
      </div>
      
      <div id="mobile-cart" style="display: none">
		<ul>
		{% for item in cart.items %}
			<li class="cf">
				<div class="forty l">
				<a href="{{item.product.url}}"><img src="{{ item.product.images.first | product_img_url: 'medium' }}" alt="{{ item.title | escape }}" width="160" height="240" /></a>
	    		</div>
	    		<div class="sixty r">
	    			<span class="carttitle"><b><a href="{{item.product.url}}">{{ item.product.title }}</a></b></span>
	    			{% if item.variant.option1 != 'Default Title' and item.variant.option1 != 'Default' %}
					  {% for i in (1..item.product.options.size) %}
					    {% case i %}
					    {% when 1 %}
					      <span><b>{{ item.product.options.first }}:</b> {{ item.variant.option1 }}</span>
					    {% when 2 %}			
					      {% if item.product.options.size == 2 %}
					        <span><b>{{ item.product.options.last}}:</b> {{ item.variant.option2 }}</span>
					      {% else %}
					        <span><b>{{ item.product.options | remove : item.product.options.first | remove: item.product.options.last  }}:</b> {{ item.variant.option2 }}</span>
					      {% endif %}
					    {% else %}  
					      <span><b>{{ item.product.options.last }}:</b> {{ item.variant.option3 }}</span>
					    {% endcase %}
					  {% endfor %}
					{% endif %}
	    			<span class="cartprice"><b>PRICE:</b> {{ item.variant.price | money }}</span>
	                <span class="cartquantity"><b>QTY:</b> {{ item.quantity }}</span>
	                <a href="/cart/change?line={{ forloop.index }}&quantity=0" class="cartremove" title="remove">x Remove</a>
	    		</div>
			</li>
		{% endfor %}
		</ul>
		<h4 class="subtotal">Subtotal: {{ cart.total_price | money }}</h4>
		<a href="/cart" class="btn view-cart">View Cart</a>

      </div>
      
      <div class="scarf showmobile">
       	<a href="/search" class="search-btn al">
          <i class="fa fa-search"></i> Search
      	</a>
		
        {% if shop.customer_accounts_enabled %}
	      <p class="customer-links"> 
	        {% if customer %}
	          <a href="/account">My Account</a> |  {{ 'Log out' | customer_logout_link }}
	        {% else %}
	          {{ 'Login' | customer_login_link }}
	        {% endif %}
	      </p>
	      {% endif %}
	      <form action="/search" method="get" id="search-form" role="search">
	      	
	        <input name="q" type="text" id="search-field" placeholder="" class="hint" />
	        <input type="submit" value="Search Store" />
	        <input type="hidden" name="type" value="product" />
        </form>
      </div>

    </header>

    <div id="main" role="main" class="cf">
      {{ content_for_layout }}
    </div>
    
    <div id="mailing-list">
        <h4 class="hidemobile">{{mailing_list}}</h4>
      	<h4 class="showmobile" style="display: none;">{{mailing_list}}<br/></h4>
          <form id="signup" class="hidemobile" target="_blank" name="mc-embedded-subscribe-form" method="post" action="http://unyq.us8.list-manage.com/subscribe/post?u=eb5c98aa502b7c96a52877dc5&amp;id=88bb468eb2">
            <input type="email" value="" class="email" id="EMAIL contact_email" placeholder="{{enter_email}}" >
            <input id="signup-submit" type="submit" value="JOIN" name="Submit" />
          </form>
		<div id="footer-social">
			{% if settings.facebook %}<a href="{{settings.facebook_url}}"><i class="fa fa-facebook"></i></a>{% endif %}
			{% if settings.twitter %}<a href="{{settings.twitter_url}}"><i class="fa fa-twitter"></i></a>{% endif %}
			{% if settings.instagram %}<a href="{{settings.instagram_url}}"><i class="fa fa-instagram"></i></a>{% endif %}
			{% if settings.pinterest %}<a href="{{settings.pinterest_url}}"><i class="fa fa-pinterest"></i></a>{% endif %}
			{% if settings.youtube %}<a href="{{settings.youtube_url}}"><i class="fa fa-youtube"></i></a>{% endif %}
		</div>
    </div>
  
    <div style="display: none">
        <div id="newsletter-popup">
          <h4>{{mailing_list}}</h4>
            <form id="p-signup" target="_blank" name="mc-embedded-subscribe-form" method="post" action="http://unyq.us8.list-manage.com/subscribe/post?u=eb5c98aa502b7c96a52877dc5&amp;id=88bb468eb2">
                <input type="email" value="" name="EMAIL" class="email" id="p-EMAIL" placeholder="{{enter_email}}">
                <input id="p-signup-submit" type="submit" value="JOIN" name="Submit" />
            </form>
        </div>        
    	</div>
    <footer>
    
      

		      	
  <div>
	{% for link in linklists.footer.links %}
	<a href="{{ link.url }}" title="{{ link.title }}"{% if link.active %} class="active"{% endif %}>{{ link.title }}</a> {% unless forloop.last %} | {% endunless %}
	{% endfor %}
      
  </div>
  <div>
	{% for link in linklists.footershopping.links %}
	<a href="{{ link.url }}" title="{{ link.title }}"{% if link.active %} class="active"{% endif %}>{{ link.title }}</a> {% unless forloop.last %} | {% endunless %}
	{% endfor %}
  </div>


	      
      <p align="center" role="contentinfo" class="copyright cf">Copyright &copy; {{ "now" | date: "%Y" }} {{ shop.name }}. {{rights}}</p>
	           
	           <p  align="center">
                 <img src="{{'cards.gif' | asset_url }}" alt="Payment Methods - American Express, Visa, Mastercard, Paypal" width="186" height="24" />
	           </p>
      <a id="dothepopup" class="fancybox" href="#newsletter-popup" style="position: absolute; bottom: 0; left: 0; width: 10px; height: 10px; color: black;">UNYQ Newsletter</a>
    </footer>

    {% comment %}Shopify does not support Internet Explorer 6.{% endcomment %}
    <!--[if lt IE 7]>
    <p class="chromeframe">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> or <a href="http://www.google.com/chromeframe/?redirect=true">activate Google Chrome Frame</a> to improve your experience.</p>
    <![endif]-->
    
  
  {% unless template.handle contains 'collection' %}
  <script>
  $(function() {
  	$('.lazy').lazyload({
            effect: "fadeIn",
            threshold: 200
    });
    var hash = $(location).attr('hash');
    
    if(hash) {
		$.cookie('language', hash, { expires: 7, path: '/' });
    }
    
    if($.cookie('language') == '#en') {
      console.log('stay in english, please');
    } else {
      console.log('pick my language');
      $.ajax({
        url:'//freegeoip.net/json/',
        type: 'post',
        dataType: 'jsonp',
        success: function(location) {
          //console.log(location.country_code);
          if(location.country_code==='ES' || 
             location.country_code==='CO' || 
             location.country_code==='PE' ||
             location.country_code==='VE' ||
             location.country_code==='EC' ||
             location.country_code==='GT' ||
             location.country_code==='CU' ||
             location.country_code==='BO' ||
             location.country_code==='HN' ||
             location.country_code==='PY' ||
             location.country_code==='SV' ||
             location.country_code==='CR' ||
             location.country_code==='PA' ||
             location.country_code==='GQ' ||
             location.country_code==='MX' ||
             location.country_code==='AR' ||
             location.country_code==='CL' ||
             location.country_code==='DO' ||
             location.country_code==='NI' ||
             location.country_code==='UY') {
                 window.location="http://es.unyq.com"; 
             }
          if(location.country_code==='DE' || 
             location.country_code==='BE' || 
             location.country_code==='AT' ||
             location.country_code==='CH' ||
             location.country_code==='LU' ||
             location.country_code==='FL') {
                 window.location="http://de.unyq.com"; 
             }
        }    
      });
    } 
  });
  </script>
  {% endunless %}
  <script type="text/javascript" src="//s7.addthis.com/js/300/addthis_widget.js#pubid=ra-4f79fb1b2d133ec3" async></script>
  <div id="fb-root"></div>
  <script>(function(d, s, id) {
    var js, fjs = d.getElementsByTagName(s)[0];
    if (d.getElementById(id)) return;
    js = d.createElement(s); js.id = id;
    js.src = "//connect.facebook.net/en_US/sdk.js#xfbml=1&appId=213869252107195&version=v2.0";
    fjs.parentNode.insertBefore(js, fjs);
  }(document, 'script', 'facebook-jssdk'));</script>
  {% include 'adroll-pixel' %}
  {% include 'twitter-tracking' %}
  {% include 'currencies' %}
  
    {% if template contains 'cart' %}
     {% include 'google-cart' %}
  {% endif %}
  

  <script type="text/javascript" src="//assets.zendesk.com/external/zenbox/v2.6/zenbox.js" async></script>
  {% include 'zopim-chat' %}

  <script>
      var cb = function() {
        var l = document.createElement('link'); l.rel = 'stylesheet';
        l.href = "//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css";
        var h = document.getElementsByTagName('head')[0]; h.parentNode.insertBefore(l, h);
      };
      var raf = requestAnimationFrame || mozRequestAnimationFrame ||
          webkitRequestAnimationFrame || msRequestAnimationFrame;
      if (raf) raf(cb);
      else window.addEventListener('load', cb);
    </script>

</body>
</html>